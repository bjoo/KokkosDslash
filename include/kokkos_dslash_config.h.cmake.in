#pragma once

// Do we have QDP++?
#cmakedefine HAVE_QDPXX @HAVE_QDPXX@

// Do we have Kokkos
#cmakedefine HAVE_KOKKOS @HAVE_KOKKOS@

#cmakedefine MG_FLAT_PARALLEL_DSLASH
#cmakedefine MG_KOKKOS_USE_MDRANGE

// What is our Default Loglevel (do we need this still ? )
#cmakedefine MG_DEFAULT_LOGLEVEL @MG_DEFAULT_LOGLEVEL@

// Do we use a parscalar Arch? 
#cmakedefine MG_PARSCALAR_ARCH  @MG_PARSCALAR_ARCH@

// Can we configure the vector length?
#cmakedefine MG_VECLEN_SP   @MG_VECLEN_SP@

// AVX512	
#cmakedefine MG_USE_AVX512 @MG_USE_AVX512@

// AVX2 
#cmakedefine MG_USE_AVX2 @MG_USE_AVX2@

// CUDA
#cmakedefine MG_USE_CUDA @MG_USE_CUDA@

// HIP
#cmakedefine MG_USE_HIP @MG_USE_HIP@
